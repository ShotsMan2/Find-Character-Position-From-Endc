# Find-Character-Position-From-Endc
A C function that searches for a target character within a string. It calculates the string length manually and iterates backwards to return the character's 1-based position relative to the end of the string.
